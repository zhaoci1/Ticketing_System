server:
  port: 8003
  servlet:
    context-path: /batch
spring:
  datasource:
    username: train_batch
    url: jdbc:mysql://rm-7xv9s40yi6w07v136go.rwlb.rds.aliyuncs.com/train_batch?characterEncoding=UTF8&autoReconnect=true&serverTimezone=Asia/Shanghai
    password: 202019Cs
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
logging:
  level:
    com:
      jiawa:
        train:
          batch:
            mapper: trace