spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848   # nacos server地址
        file-extension: yaml    # 配置中心文件后缀
  profiles:
    active: dev   # 启动环境
  application:
    name: member #    注册中心的名字